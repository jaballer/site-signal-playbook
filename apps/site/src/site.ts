/** Site-wide variables. Content-level facts belong in /content instead. */
export const site = {
  name: "Site Signal Playbook",
  tagline: "SEO + AI search",
  version: "v2",
  storageKeys: {
    theme: "ssp-theme",
    checklist: "ssp-checklist",
  },
} as const;
